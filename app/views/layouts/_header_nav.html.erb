<nav class='navbar navbar-default navbar-fixed-top' role='navigation'>
	<div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#right-nav">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#/assessment/policy">Assessment</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="right-nav">
      <ul class="nav navbar-nav navbar-right">
        <% if signed_in? %>
          <li><%= link_to '/#/profile' do %>
            <%= content_tag(:span, '', class: "glyphicon glyphicon-user").html_safe %>
            <%= 'Account' %>
          <% end %>
          </li>
          <li><%= link_to logout_path, method: :delete do %>
            <%= content_tag(:span, '', class: "glyphicon glyphicon-log-out").html_safe %>
            <%= 'Logout' %>
           <% end %>
           </li>
        <% else %>
          <li><%= link_to 'Login', login_path %></li>
          <li><%= link_to 'Sign Up', signup_path %></li>
        <% end %>
      </div>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>